# Phase 1, 2 & 3: Manim Rendering Node - Python Dependencies

# Core Manim library
manim==0.18.0

# FastAPI web framework for asynchronous API orchestration
fastapi==0.109.0

# ASGI server for production deployment
uvicorn[standard]==0.27.0

# Request validation and serialization
pydantic==2.5.3

# Phase 2: State Management - Upstash Redis SDK
upstash-redis==0.15.0

# Phase 3: LLM Orchestration - Mistral AI SDK
mistralai==1.0.0

# Phase 4: Database & Storage - Supabase Client
supabase==2.18.0

# Environment variable management
python-dotenv==1.0.0

# Additional Manim dependencies (compatible versions)
# Note: Manim 0.18.0 requires Pillow<10.0, so we let it pull compatible versions
# These will be auto-installed with manim, but listed for clarity

# Python multipart for handling file uploads (if needed in future)
python-multipart==0.0.6

# Logging and monitoring
python-json-logger==2.0.7

# Requests for testing
requests==2.31.0
